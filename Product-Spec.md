# Product Spec — iPhone 英语学习浏览器

**生成日期**：2026-05-19
**版本**：v2.11
**状态**：v2.11 听音翻译方案确立（**推翻 v2.10 的「iOS 不可行」误判**）——找到并端到端验证了可行路径：**后端按 videoId 取 YouTube 音频流（InnerTube `streamingData` 的纯音频 `adaptiveFormats`，明文 URL、任何视频都有、含无字幕 Shorts）→ 自部署 Whisper 离线语音识别（仅听写英文、不翻译）→ 走现有大模型分层翻译 → 双语叠层**。已用真实无字幕 Shorts（Suits 剪辑 `2QtsWjF3e78`）端到端验证：取到音频流、Whisper 准确识别出完整英文台词（含屏幕台词 "sleep with your sister"）、可翻译，全链路通。这绕开了 v2.10 卡死的 `captureStream`（WKWebView 采不到播放中音频）——**不在前端采集，而是后端拉音频流**。听音翻译复活，最终形态为**混合**：有字幕轨的视频读字幕翻译（Phase 8.9 已落地），无字幕轨的视频走音频流 ASR，合起来**任何视频可翻**（追平竞品 Immersive Translate）。**两条路的翻译环节统一走现有大模型分层**：ASR（自部署 Whisper）只负责「英文语音→英文文字」、零 API 成本、不做翻译；翻译「英文→中文」由**产品方后端配置的大模型**完成，**按订阅权限路由、用户在权限内可选模型**（Free→translation-proxy 便宜模型如 DeepSeek、Pro/Max→model-gateway 更优模型如 OpenAI；现有 `model-catalog` options + `preferredModelId` 机制已支持，听音 route 已接入）。合规取舍（**实质边界变化**）：后端实时拉取音频流片段做识别（与竞品同类机制），仅取当前识别所需、不持久化整轨、不缓存、不再分发——**放宽了原「不下载音视频」边界**，存在 YouTube ToS 风险（竞品同担），但不碰版权存储 / 离线整片 / 再分发。v2.10（已被本 v2.11 推翻，保留作沿革：曾因 `captureStream` 在 WKWebView 采不到播放中音频，误判 iOS 听音整体不可行——错在只试了前端采集这一条路，未发现后端拉音频流可行）。v2.9 听音翻译升级——YouTube 视频读不到字幕轨（captionTracks=0，如影视 / 音乐 / 版权剪辑）时自动切换到听音识别翻译（整句完整识别后输出双语、不逐词滚动，对齐沉浸翻译类竞品「几乎任意视频可翻」的体验）；听音额度不变（Free 每天 10 分钟、Pro / Max 更高额度），Pro / Max 高额度成本由未来订阅覆盖，但订阅 / 支付系统实现仍延后（见 non-goals）。v2.8 视频字幕改用「读取视频自带字幕轨数据」（不依赖用户手动开 CC、覆盖 Shorts 与横屏，详见「YouTube 边界」字幕来源）。v2.7 Free 翻译后端改用产品方服务端配置的便宜大模型（详见「AI 服务与模型等级」）。v2.6 YouTube 整站重定位（修正 v2.5）：真机验证发现 v2.5 只做了视频播放页隐形态，YouTube 首页 / 列表仍被当普通文本网页、套了“原文 / 双语 / 学习”控件 + 浏览工具条，且注入脚本破坏了 YouTube 原生滑动 / 点击 / 路由。本次把 YouTube 从“普通可翻译网页 + 视频页特殊”重定位为“专门适配的视频站点”：整站（首页 / 列表 / 搜索 / Shorts / 视频页）保持原生 YouTube 操作体验，App 在 YouTube 整站几乎隐形，唯一增强是视频播放时叠一层可开关的双语字幕（召唤式）；YouTube 任何页面都不套阅读显示模式控件 / 常驻工具条；本版不做 YouTube 页面文字翻译（标题 / 搜索 / 评论）；注入必须 SPA 友好、绝不破坏 YouTube 原生交互。v2.5 的视频页隐形态 / 召唤态 + 双语字幕、Free 翻译开箱即用、BYOK 边界澄清均保留；听音翻译仍为 Free 每天 10 分钟。（Free 翻译后端见「AI 服务与模型等级」：已从第三方通用翻译改为产品方在服务端配置的便宜大模型，沿用独立 translation-proxy、与付费 gateway 解耦，用户无感仍是 free translation。）

---

## 产品概述

**一句话定义**：一个给中文用户用的 iPhone 英语学习浏览器，让用户刷 YouTube、Reddit、Wikipedia、AO3、X 等真实英文内容时，自动变成中英对照、可点词、可收藏、可复习的学习材料。

**目标用户**：中文母语者，英语阅读和听力能力不稳定，平时愿意刷 YouTube、Reddit、Wikipedia、AO3、X、英文新闻或论坛，但遇到英文内容经常看不懂、懒得复制到翻译软件、也很难把看过的词句沉淀下来复习。

**核心价值**：让用户在手机上继续刷自己本来就想看的真实内容，同时顺手完成理解、查词、收藏和复习。产品不是再造一套课程，而是把用户已经在消费的英文网页变成学习环境。

**差异化定位**：

- 相比 Immersive Translate：不追求做最全平台翻译工具，重点做 iPhone 端学习闭环。
- 相比 Language Reactor / Trancy：不把视频字幕作为唯一主场，但**整个 YouTube 站点**必须像用户正常刷 YouTube 一样操作，App 在整站几乎隐形，只在视频播放时增加一层可开关的轻量双语字幕辅助。
- 相比 Readlang：不只做文章阅读器，而是做移动端真实网页浏览器，支持 YouTube、Reddit、Wikipedia、AO3、X 等高频内容场景。
- 相比普通翻译 App：翻译不是终点，点词点句、上下文解释、收藏、复习才是产品的长期价值。

**本版聚焦（v2.6）**：定位不变，仍是多平台英语学习浏览器；本版集中把 **YouTube 整站体验**做对——整站原生、App 隐形、仅视频播放页叠可开关双语字幕、整站不套阅读显示模式控件、注入不破坏交互；本版不做 YouTube 页面文字翻译。其余平台（Reddit / Wikipedia / AO3 / X）保留在产品定位中、开发上延后。

## 应用场景

- **刷 YouTube 学英语**：用户在 App 内打开 YouTube，整站（首页 / 列表 / 搜索 / Shorts / 视频页）都像原生 YouTube 一样滑动、播放、点赞、评论和切换视频，App 在 YouTube 几乎隐形、绝不打断或破坏原生操作。进入视频播放页后，App 唯一的增强是在视频画面安全区域叠一层可开关的双语字幕（英文 + 中文），操作收进召唤式精简浮层；首页 / 列表 / 搜索不做翻译、不套任何阅读显示模式控件或浏览工具条。视频字幕优先读取视频自带的字幕轨数据（含自动生成字幕、无需用户手动开 CC，覆盖 Shorts 与横屏视频），按播放进度显示当前句；视频没有字幕轨或质量明显不足时，自动或由用户切换到“听音翻译 Beta”。视频字幕文本翻译走 Free 默认翻译（产品方服务端配置的便宜大模型），开箱即可使用；听音翻译成本高，Free 每天 10 分钟，Pro / Max 提供更高额度。
- **读 Reddit 讨论**：用户打开 Reddit 英文帖子，系统把主贴和评论按段落插入中文解释；用户点开俚语、梗、缩写时看到中文语境解释，而不是只得到字典释义。
- **读 Wikipedia / 新闻文章**：用户打开长篇英文内容，开启阅读模式后保留原文结构，并在段落下方显示中文；用户可以把关键句保存为句子卡。
- **看 AO3 / 英文故事**：用户阅读英文小说或同人内容时，系统按段落翻译，允许用户隐藏中文先读英文，卡住再点开中文。
- **碎片化复习**：用户第二天打开 App，不需要重新找网页，直接看到昨天收藏的词、短语、句子和来源上下文，用主动回忆方式复习。

## 功能需求

### 核心功能

- **首页快捷入口**：用户打开 App → 看到搜索 / 输入网址框和常用站点入口 → 快速进入 YouTube、Reddit、Wikipedia、AO3、X、收藏夹。
- **内置浏览器**：用户输入网址或点击站点入口 → 系统用内置 WebView 打开网页 → 用户在 App 内完成浏览，不需要跳出到 Safari。
- **网页双语翻译**：用户在文章、帖子、评论等文本型网页点击翻译按钮 → 系统识别当前页面主要文本 → 在英文段落下方插入中文翻译。 [⚡ AI 增强]
- **阅读显示模式**：用户在真正的文本型网页（Reddit / Wikipedia / AO3 / 文章等）切换显示模式 → 系统在原文、双语对照、学习模式之间切换 → 用户可以先看英文，必要时再展开中文；该模式仅用于文本型网页，不用于 YouTube 的任何页面（首页 / 列表 / 搜索 / Shorts / 视频页都不展示）。
- **点词点句解释**：用户点击单词、短语或选中句子 → 系统显示中文释义、当前语境解释、例句和收藏按钮。 [⚡ AI 增强]
- **收藏学习材料**：用户点击收藏 → 系统保存词、短语、句子、来源网页标题、来源 URL、上下文和翻译 → 后续可在收藏页和复习页查看。
- **主动回忆复习**：用户进入复习页 → 系统展示已收藏材料，默认先隐藏中文 → 用户先回忆再查看答案，并标记记住 / 模糊 / 不会。 [⚡ AI 增强]
- **YouTube 视频沉浸翻译**：用户在内置浏览器打开 YouTube watch / Shorts 页面 → 系统进入视频沉浸翻译模式，App 自身 UI 整体隐形、不在屏幕底部常驻任何工具条，YouTube 独占播放区域；用户需要操作时通过召唤式精简浮层（翻译开关、字幕 / 听音来源切换、收藏当前句、返回）控制 → 默认读取视频自带的字幕轨数据（含自动生成字幕、无需手动开 CC、覆盖 Shorts 与横屏）生成“英文原句 + 中文翻译”的轻量叠层、随播放进度滚动；视频没有字幕轨（captionTracks=0，常见于影视 / 音乐 / 版权剪辑）、质量明显不足或用户手动选择时，**自动切换到“听音翻译”**（无需用户手动开启），通过音频识别按句生成原文和中文翻译——识别一句完整话后整句输出原文 + 整句翻译显示（不逐词滚动），对齐沉浸翻译类竞品「几乎任意视频可翻」的体验。视频字幕文本翻译走 Free 默认翻译（产品方服务端配置的便宜大模型）、开箱可用；听音翻译 Free 每天限制 10 分钟，额度用完后回退到字幕翻译或轻量提示。本版只在视频播放页做字幕翻译，不做 YouTube 首页 / 列表 / 搜索的页面文字翻译。 [⚡ AI 增强]
- **账号、服务等级与隐私控制**：用户进入设置 → 查看游客 / 已登录状态、当前服务等级、可用模型档位、目标语言、数据保留策略和清除历史 → 用户能控制自己的浏览和学习数据，但不配置第三方 Provider、API Key 或自带模型。

### 辅助功能

- **站点快捷管理**：用户编辑首页快捷入口 → 系统允许添加、删除、排序常用站点 → 首页更符合个人使用习惯。
- **学习历史**：用户进入历史页 → 系统展示最近翻译过的网页和收藏数量 → 用户可回到原网页继续学习。
- **基础统计**：用户查看统计 → 系统显示今日翻译页数、收藏数、复习完成数、连续使用天数 → 用于自用验证，不做游戏化奖励。
- **失败降级提示**：翻译失败、网页结构无法识别或字幕不可访问时 → 系统明确提示原因，并允许用户选择文本选区翻译或复制文本翻译。
- **导出收藏**：用户导出收藏 → 系统导出 CSV / Markdown → 方便后续迁移到 Anki 或其他笔记工具。

## UI 布局

### 整体布局

产品是 iPhone 优先的移动 App，采用底部 Tab 导航。主入口不是课程列表，而是浏览器首页。

底部 Tab：

- **浏览**：首页、地址栏、快捷站点、内置网页浏览。
- **收藏**：收藏的单词、短语、句子、网页来源。
- **复习**：主动回忆卡片和复习队列。
- **设置**：账号状态、服务等级、模型档位、目标语言、隐私、数据清理。

### 主要页面

- **浏览首页**：顶部为 App 标识和设置入口；中部为搜索 / 输入网址框；下方为常用站点快捷入口；底部为 Tab。
- **网页浏览页**：主体为 WebView；进入网页后 App 底部 Tab 不应长期挤占浏览内容。文本型网页可显示轻量浏览工具条，包含返回、前进、翻译、阅读显示模式、收藏、更多操作。**YouTube 整站（首页 / 列表 / 搜索 / Shorts / 视频页）不走此文本网页工具条与阅读显示模式，按原生体验处理，仅视频播放页叠双语字幕。**
- **YouTube 视频页**：主体为 YouTube 原网页 / 播放器，占满屏幕；进入视频播放页后 App 自身 UI 默认整体隐形（隐形态），不得在屏幕底部或顶部常驻任何工具条 / 状态栏 / 分段控件，不得用 App 面板挤压或上移 YouTube 播放区域与原生控件。用户需要时通过明确手势或一个低存在感的召唤入口唤出召唤态：一层精简浮动菜单（翻译开关、字幕 / 听音来源切换、收藏当前句、返回、必要错误提示），操作完成后自动回到隐形态。视频画面上的双语字幕叠层属于内容层、可常驻，不计入上述 App 控件。
- **网页翻译结果层**：中文翻译直接插入原文附近，不使用独立大弹窗覆盖主要内容；点词点句解释用底部抽屉展示。
- **视频字幕叠层**：仅用于 YouTube 视频播放页，贴近视频画面安全区域显示英文原句和中文翻译。字幕来源可以是“字幕翻译”或“听音翻译 Beta”，但用户看到的是同一套双语字幕体验。叠层必须避开 YouTube 控件、频道信息、广告、品牌标识和系统安全区；无法避开时降级为视频下方轻量字幕条或提示。
- **收藏页**：按词、短语、句子、网页来源分类；支持搜索、筛选和批量删除。
- **复习页**：卡片式主动回忆，不做复杂课程路径；每张卡保留来源上下文。
- **设置页**：优先展示账号状态、语言、服务等级、可用模型档位、隐私和数据清理；不提供高级 Provider 配置或自带 API Key 输入。

### 控件规范

- **翻译按钮**：网页浏览页常驻主操作，允许一键翻译 / 重新翻译。
- **阅读显示模式切换**：文本型网页使用分段控件，选项为原文、双语、学习；YouTube 视频播放页不展示该分段控件。
- **视频字幕控制**：YouTube 视频播放页不使用常驻工具条，也不使用阅读模式分段控件；翻译开关、字幕 / 听音来源切换、重试、收藏当前句等操作收纳进召唤态精简浮动菜单，默认隐形、按需唤出、用完即隐。
- **收藏按钮**：出现在点词点句解释抽屉和段落操作中。
- **复习反馈**：使用三段按钮，选项为记住、模糊、不会。
- **站点入口**：使用图标 + 文本，不把站点列表做成课程卡片。

## 用户使用流程

路径关系：首次配置、日常浏览、复习是顺序关系；日常浏览和复习可重复循环。

### 路径 1：首次使用

1. 用户打开 App。
2. 系统向后端创建或恢复匿名游客会话，默认获得 Free 等级和基础额度；会话令牌只保存在 Keychain。
3. 系统展示产品定位：这是英语学习浏览器，不是普通翻译器。
4. 用户选择母语和目标语言，默认中文 → 英语。
5. 系统展示当前账号状态为游客模式、默认 Free 服务等级和可用模型档位。
6. 系统提示浏览历史、收藏、翻译内容的数据保存策略。
7. 用户进入浏览首页，看到 YouTube、Reddit、Wikipedia、AO3、X 等快捷入口。

### 路径 2：边刷边学

1. 用户点击 YouTube / Reddit / Wikipedia 或输入网址。
2. 系统打开内置浏览器。
3. 如果当前是文本型网页，用户点击翻译按钮。
4. 系统识别页面文本并插入中英对照。
5. 用户遇到不懂的词或句子，点词 / 选句。
6. 系统展示中文语境解释和收藏按钮。
7. 用户收藏有价值的词句。
8. 系统保存来源上下文，后续进入复习队列。

### 路径 2A：刷 YouTube 视频

1. 用户点击 YouTube 快捷入口或打开 YouTube 链接。
2. 系统进入全屏浏览状态，尽量保留 YouTube 原有浏览和播放体验。
3. 用户打开 watch / Shorts 视频页。
4. 系统识别页面类型，切换到视频沉浸翻译模式：App 自身 UI 整体隐形，不在屏幕上下常驻任何工具条，YouTube 独占播放区域。
5. 系统读取视频自带的字幕轨数据（含自动生成字幕、无需手动开 CC），用 Free 默认翻译（产品方服务端配置的便宜大模型）翻译，并在视频画面安全区域按播放进度显示英文原句和中文翻译（Free 无需登录、无需配置即可使用文本翻译）。
6. 用户继续按 YouTube 原方式播放、暂停、滑动、点赞、评论或切换视频；需要控制翻译时，用明确手势或召唤入口唤出精简浮动菜单，操作完用完即隐。
7. 用户在浮动菜单点选收藏当前句，系统保存视频标题、URL、时间位置、原文句子、字幕 / 听音来源和中文翻译。
8. 当字幕不可用或质量明显不足时，系统自动或由用户切换到“听音翻译 Beta”；Free 用户每天可使用 10 分钟听音翻译，额度不足、音频识别失败或模型服务失败时只显示轻量提示，并保留页面文字翻译和选区解释能力。

### 路径 3：第二天复习

1. 用户进入复习页。
2. 系统展示昨天和最近收藏的词句，默认隐藏中文。
3. 用户先回忆意思，再点击查看答案。
4. 用户标记记住、模糊或不会。
5. 系统根据反馈调整后续复习优先级。

### 路径 4：登录与测试等级

1. 用户进入设置页。
2. 游客用户可继续使用 Free，也可选择登录以绑定账号和未来恢复权益。
3. 公开版本登录必须支持 Sign in with Apple；如提供 Google 登录，也必须同时提供符合 App Store 要求的等价隐私登录选项。
4. 开发 / staging 环境可显示 Pro / Max 测试账号入口，用于验证模型锁定态、额度和高等级模型；生产环境不得显示或接受测试账号登录。
5. 登录成功后，后端返回会话令牌、当前等级、额度和模型目录；App 存入 Keychain 并刷新设置页。
6. 用户退出登录后回到游客 Free 会话；本机学习数据默认保留，除非用户主动清理。

## AI 能力需求

| 能力类型 | 用途说明 | 应用位置（对应功能名） |
|---------|---------|------------------------|
| 便宜大模型翻译（Free 默认） | 用产品方在服务端配置的便宜大模型（OpenAI 兼容 Chat Completions，如 DeepSeek V3 等）把文本型网页文本、YouTube 视频字幕文本翻成中文；开箱即用、跨端、与付费大模型后端解耦 | 网页双语翻译 / YouTube 视频字幕翻译（Free 档） |
| 文本理解与生成（Pro / Max 增强） | 用更强的大模型生成更自然、更贴合上下文的中文翻译，质量高于 Free 档的便宜模型 | 网页双语翻译 / YouTube 字幕翻译（Pro / Max 档） |
| 文本理解与生成 | 对单词、短语、句子给出中文语境解释、例句和简短用法说明 | 点词点句解释：用户点词或选句后 |
| 文本理解与生成 | 把收藏内容整理成主动回忆卡片，生成可复习的问题和答案 | 主动回忆复习：生成复习队列时 |
| 语音识别与翻译 | 在字幕不可用、质量明显不足或用户手动选择时，对当前播放音频做短片段识别并生成英文原文和中文翻译；Free 每天限制 10 分钟 | YouTube 听音翻译 Beta |
| 工具调用 | 在用户允许的情况下调用 Free 翻译代理、后台模型网关或本地缓存，避免重复翻译 | 网页双语翻译 / 点词点句解释 |

## 技术方向

| 维度 | 选择 | 理由 |
|------|------|------|
| 产品类型 | Mobile | 用户明确要苹果手机端，核心场景是手机刷网页和视频页面。 |
| 后端需求 | 大模型 / 账号必需；Free 文本翻译走独立代理 | Pro / Max 大模型翻译、点词解释、学习卡、听音 ASR、账号与权益判定，必须通过自有 model-gateway 统一保存密钥、维护模型目录、控制额度、创建游客会话、校验登录身份并路由。但 Free 文本翻译走独立的轻量翻译代理（translation-proxy，内部调用产品方服务端配置的便宜大模型、OpenAI 兼容，与付费 model-gateway 解耦）——付费大模型后端没部署或故障时，Free 文本翻译仍可开箱使用。 |
| 推荐技术栈方向 | iOS 原生 Swift / SwiftUI + WKWebView | 需要稳定内置浏览器、脚本注入、原生收藏复习体验和 App Store 交付。 |
| 数据存储 | 本地学习数据 + 后端账号 / 服务状态 | MVP 本地保存浏览历史、收藏、复习状态和缓存；后端保存匿名会话、账号身份、服务等级、额度、用量、模型目录和 Provider 密钥；不在 App 内保存第三方模型密钥。 |
| 部署方式 | TestFlight，后续 App Store | 第一阶段先自用验证，稳定后再按 App Store 审核要求收口。 |
| 多端方向 | 本版 iOS，后续扩展 Web 等 | 后续要做 Web 等多端，翻译能力选型（尤其 Free 翻译代理）不绑死苹果端上框架，优先选可跨端复用的服务 / 接口。 |

## 技术说明

### WebView 与脚本注入

iOS 端使用 WKWebView 承载网页。页面文本识别和翻译层插入可通过 WKUserScript / UserContentController 完成，但必须保证脚本不会破坏网页原有交互。

### YouTube 边界

MVP 不做 YouTube 替代客户端，不下载完整视频，不后台播放，不去广告，不修改播放器核心功能，不离线保存 / 再分发音视频。（听音翻译为后端按播放进度实时拉取音频流片段做识别、用完即弃，仅取当前所需，不等同于"下载视频 / 离线分离整片"，合规边界见「YouTube 边界」听音段。）

YouTube 第一版做“学习增强”，不是 YouTube 替代客户端。视频页体验目标是：用户仍像刷 YouTube 一样使用原站，只多看到一层可关闭的双语学习辅助。

YouTube 第一版支持边界：

- YouTube 整站（首页 / 列表 / 搜索 / Shorts / 视频页）保持原生操作体验，App 在 YouTube 整站几乎隐形；不在 YouTube 任何页面套阅读显示模式控件（原文 / 双语 / 学习）或常驻浏览工具条。
- 本版不做 YouTube 页面文字翻译（标题 / 简介 / 评论 / 搜索结果）；App 在 YouTube 的唯一文本增强是视频播放页的双语字幕。
- 注入脚本必须 SPA 友好：YouTube 是单页应用（点视频 / Shorts 是前端路由、页面不重载），注入必须监听前端路由变化、不做全量 DOM 扫描、不注册干扰原生滚动 / 点击 / 手势的全局事件、overlay 不破坏 YouTube 布局；绝不破坏 YouTube 原生交互（滑动列表、点开视频 / Shorts 必须正常）。
- watch / Shorts 页面进入视频沉浸翻译模式，App 自身 UI 整体隐形、不在屏幕常驻工具条，操作收进召唤态精简浮层。
- 视频有字幕轨（含自动生成字幕）时优先读取字幕轨数据、按播放进度显示双语字幕叠层；不依赖用户手动开 CC，覆盖 Shorts 与横屏视频。
- 字幕不可用（captionTracks=0 或解析 0 句）、质量明显不足或用户手动选择时，**自动切换到“听音翻译”**（读不到字幕轨即自动切，无需用户手动开启）；识别一句完整话后整句输出原文 + 中文翻译（不逐词滚动），双语叠层与字幕翻译共用同一套体验。Free 每天限制 10 分钟，Pro / Max 提供更高额度（高额度成本由未来订阅覆盖，订阅 / 支付系统实现延后，见 non-goals）。
- 听音翻译必须用户可见、可关闭、可停止、按分钟计量；后端按播放进度实时拉取该视频的音频流片段做识别（与竞品同类机制），音频片段只用于当前识别请求，默认不持久化、不保存完整音频、不缓存整轨、不再分发；不做离线整片下载转写。识别（Whisper）只产出英文文本，翻译走大模型分层（按订阅权限路由 / 用户选模型）。
- 字幕或听音能力失败时必须明确提示，不能伪装成功。
- 字幕来源（字幕轨翻译路径）：仅在用户播放当前视频时，实时读取该视频自带的字幕轨数据（YouTube 播放器 / 页面已提供的字幕轨，同源、跟随播放进度）用于翻译显示；只取当前播放所需，不保存为字幕文件、不离线缓存整轨、不再分发或搬运。此为「有字幕轨」路径的边界；「无字幕轨」走听音路径（后端实时拉音频流片段识别，边界见上条听音段）。不替换、遮挡或禁用 YouTube 播放器原生控件、广告、链接、频道信息或品牌标识。
- 如果视频画面上无法安全显示字幕叠层，必须降级为视频下方轻量字幕条或仅提示，不强行盖住播放器。

### App Store 边界

产品不能只是网页套壳或链接集合。必须提供原生学习能力，包括收藏、复习、历史、隐私设置、学习统计和可控的数据管理。

### 账号、会话与权益

首版不强制登录。游客模式是默认体验，用户打开 App 后即可获得 Free 会话和基础额度。登录只用于绑定账号、后续跨设备权益恢复、Pro / Max 权益和未来订阅。

| 账号状态 | 用户可见行为 | 后端处理 |
|---------|-------------|----------|
| 游客 Free | 不登录即可翻译和点词；额度较低；设置页显示游客模式 | 后端创建匿名 device/session token，按设备或匿名会话限额 |
| 正式登录 | 用户可用 Sign in with Apple 登录；如提供 Google 登录，必须同时提供等价隐私登录选项 | 后端验证 Apple / Google identity token，创建用户与 session，不接受客户端传来的普通 user id |
| Dev / staging 测试账号 | 仅开发和测试环境显示 Pro / Max 测试入口 | `ENABLE_DEV_AUTH=true` 时允许种子账号登录；生产环境关闭接口和 UI |

测试账号只用于开发和 staging，不属于正式产品能力。建议默认种子：

| 账号 | 等级 | 约束 |
|------|------|------|
| `test-pro@agentenglish.local` | Pro | 密码来自环境变量或本地 seed，不写入源码，不在生产启用 |
| `test-max@agentenglish.local` | Max | 密码来自环境变量或本地 seed，不写入源码，不在生产启用 |

正式产品不得把固定 Free / Pro / Max token 写进 App 包。App 只能保存后端签发的 session token；后端以 session / user entitlement 为准，忽略客户端请求体中的服务等级。

公开版本如果提供 Google 登录，不能只做 Google 登录。iOS 端必须同时提供 Sign in with Apple，或另一个满足 App Store 登录服务要求的等价隐私登录方式。

### AI 服务与模型等级

“不做 BYOK”指的是：不让**用户**配置自定义大模型 Provider、Base URL、模型名或 API Key（例如自带 OpenAI key）。这不限制**产品方自身**在服务端配置翻译模型——Free 的默认文本翻译正是由产品方在服务端统一配置的便宜大模型（OpenAI 兼容 Chat Completions，如 DeepSeek V3 等）提供，用户无需任何配置、开箱即用，界面上只显示 “free translation”、不暴露背后用的是哪个模型 / 厂商。用户看到的是产品化服务等级，底层翻译服务、Provider 和大模型由产品 / 后台统一维护。

| 等级 | 用户可见能力 | 后台处理 |
|------|--------------|----------|
| Free | 默认翻译（网页、YouTube 字幕文本），开箱即用、跨端、无需登录或配置；基础点词解释；较低额度和较慢速率；YouTube 听音翻译 Beta 每天 10 分钟 | 文本翻译走独立轻量翻译代理 translation-proxy（内部调用产品方服务端配置的便宜大模型、OpenAI 兼容，与付费 model-gateway 解耦）；缓存优先；听音走低成本 ASR、严格限额；额度不足时降级到字幕翻译或轻量提示 |
| Pro | 更自然的大模型网页 / 字幕翻译、点词点句解释、学习卡生成、较高额度；更高听音翻译分钟数 | 路由到低延迟 LLM / ASR 或高性价比模型；支持失败重试和同档模型 fallback |
| Max | 复杂长文、专业内容、长上下文解释、更高质量翻译和更稳定听音识别 | 路由到高质量模型 / ASR；限制高成本调用，显示用量和额度状态 |

设置页只展示“账号状态、当前等级、可用模型档位、默认模型偏好、用量状态和隐私说明”。普通用户不需要理解 Provider、Base URL、OpenAI-compatible、API Key 这类技术概念，也不需要配置任何翻译来源。

### 隐私与数据

- 默认本地保存收藏、复习记录和浏览历史。
- 用户可清除单个站点历史、全部浏览历史、全部收藏和全部 AI 缓存。
- 如启用后台 AI / 翻译服务，必须提示用户当前页面文本会发送到自有后端，并可能由后端转发给当前等级对应的第三方模型或翻译服务。
- 如启用 YouTube 听音翻译 Beta，必须提示用户当前播放音频会被按短片段发送到自有后端进行识别和翻译，并可能由后端转发给当前等级对应的 ASR / 模型服务；音频片段默认不持久化，不保存完整音频，用户可随时关闭。
- App 不提供自定义 Provider、API Key、Base URL 或 BYOK 配置入口；第三方服务密钥只允许保存在后端密钥管理环境中。
- 不默认上传完整浏览历史到自有服务器。

## 补充说明

### MVP 范围

| 项目 | 第一版处理 |
|------|------------|
| 网页双语翻译 | 必须稳定支持 |
| Free 默认文本翻译 | 必须开箱可用：网页 / 页面文字 / 字幕文本走独立轻量翻译代理（内部调用产品方服务端配置的便宜大模型），无需登录或配置，与付费大模型后端是否就绪解耦 |
| 点词点句解释 | 必须稳定支持 |
| 收藏与复习 | 必须稳定支持 |
| YouTube 整站原生体验 | 本版重点：整站（首页 / 列表 / 搜索 / Shorts / 视频页）保持原生操作，App 几乎隐形，注入 SPA 友好、绝不破坏滑动 / 点击 / 路由；任何页面不套阅读模式控件 / 工具条 |
| YouTube 页面文字翻译 | 本版不做（只做视频字幕） |
| YouTube 视频沉浸翻译 | 本版重点：视频页 App UI 隐形 + 召唤态精简浮层，砍掉底部常驻工具条，YouTube 独占屏幕，字幕翻译贴近视频安全区域 |
| YouTube 双语字幕 | 字幕翻译必须优先支持；读取视频自带字幕轨数据（含自动生成字幕、无需手动开 CC、覆盖 Shorts 与横屏）生成双语字幕、按播放进度显示 |
| YouTube 听音翻译（本版重点） | 无字幕轨视频走听音：**后端拉 YouTube 音频流 → 自部署 Whisper 识别英文 → 大模型翻译（按订阅权限路由 / 用户选模型）→ 双语叠层**，已端到端验证可行（推翻 v2.10）。整句识别后整句输出双语、不逐词滚动。Free 每天 10 分钟，Pro / Max 更高额度 |
| Reddit / Wikipedia / AO3 / X 等其他平台 | 定位保留，本版延后；优先把 YouTube 做透 |
| Free / Pro / Max 模型等级 | 必须支持用户可见的服务等级表达，至少能区分 Free 与付费模型档位 |
| 游客 Free 会话 | 必须支持；不登录也能使用基础翻译能力 |
| Dev / staging Pro / Max 测试账号 | 必须支持；仅测试环境启用，生产关闭 |
| 正式登录 | 必须预留；公开版本优先 Sign in with Apple，Google 登录只能作为并列选项 |
| 用户自定义 Provider / BYOK | 不做 |
| 前端采集播放中音频（captureStream / WKWebView 内采音） | 不做：真机实测 `captureStream()` 采不到 YouTube 跨源播放音频（`atks=0`）。听音改走**后端拉音频流 + Whisper 识别**（见状态 v2.11），不依赖前端采集 |
| 订阅 / 支付系统（StoreKit 内购） | 本版不做：Pro / Max 高额度听音的成本兜底方向预留给未来订阅，但订阅 / 支付 / 内购实现延后；本版用 dev / staging 测试账号 + Free 限额跑通听音能力，不做正式收费 |
| Netflix / Disney+ / TED / Coursera | 不做 |
| PDF / 漫画 / 图片翻译 | 不做 |
| 游戏化、打怪、装备、地图 | 全部不做 |
| 云同步学习数据 | 不做；账号只用于会话、权益和未来恢复，不上传完整学习历史 |

### 阅读显示模式

| 模式 | 说明 |
|------|------|
| 原文 | 不插入中文，只保留点词点句解释能力 |
| 双语 | 英文段落下方显示中文翻译 |
| 学习 | 默认显示英文，中文折叠，点击后展开 |

阅读显示模式只用于真正的文本型网页（Reddit / Wikipedia / AO3 / 文章等）。YouTube 的任何页面（首页 / 列表 / 搜索 / Shorts / 视频页）都不使用阅读显示模式、不展示该分段控件——YouTube 整站走原生体验，仅视频播放页叠双语字幕。

### 视频沉浸翻译模式

| 状态 | 说明 |
|------|------|
| 隐形态（默认） | 进入视频页后 App 自身 UI 整体隐藏，屏幕完全交给 YouTube；只保留视频上的双语字幕叠层（内容层）和一个低存在感的召唤入口 |
| 召唤态 | 用户用明确手势或点召唤入口后，浮出一层精简浮动菜单（翻译开关、字幕 / 听音切换、收藏当前句、返回），操作完用完即隐，回到隐形态 |
| 字幕翻译开启 | 视频画面安全区域显示英文原句和中文翻译，尽量不影响 YouTube 原生操作 |
| 听音翻译开启 | 视频画面安全区域显示听音识别出的英文原句和中文翻译，状态上标记“听音 Beta”并计入每日分钟额度 |
| 字幕翻译关闭 | 保留 YouTube 原始页面，只显示轻量翻译入口 |
| 听音识别中 | 显示轻量加载状态，不阻塞播放，允许用户随时关闭 |
| 听音额度用完 | 显示轻量提示，说明 Free 今日 10 分钟听音额度已用完，可继续用字幕翻译或页面文字翻译 |
| 字幕不可用 | 显示轻量提示，说明当前视频无字幕或无法访问字幕，并提供听音翻译入口；不影响播放 |
| 降级显示 | 无法避开播放器控件或品牌区域时，字幕显示在视频下方轻量条，而不是强行覆盖视频 |

### 复习反馈

| 反馈 | 后续处理 |
|------|----------|
| 记住 | 降低短期出现频率 |
| 模糊 | 保持近期复习 |
| 不会 | 提高近期出现频率，并保留来源上下文 |

## 非目标

- 不做传统课程 App。
- 不做考试训练。
- 不做背单词表。
- 不做游戏化 RPG 学习系统。
- 不做 YouTube / Netflix 替代客户端。
- 不做视频下载、字幕下载、后台播放、去广告。
- 不做无限制听音识别、后台听音识别（仅前台当前播放）、离线下载整片转写，或保存 / 再分发完整音频。
- 不在 YouTube 任何页面（首页 / 列表 / 搜索 / Shorts / 视频页）展示阅读型“原文 / 双语 / 学习”控件或常驻浏览工具条；不把 YouTube 当普通文本网页处理。
- 本版不做 YouTube 页面文字翻译（标题 / 搜索 / 评论）。
- YouTube 注入脚本不得破坏原生交互（滑动列表、点击、SPA 前端路由）。
- 不做全平台浏览器插件。
- 不做面向多人班级或教师管理的 LMS。
- 不让用户配置自定义大模型（自带 Provider、API Key、Base URL、模型名 / BYOK）；此条不限制产品自身集成的通用翻译服务（如 Free 默认翻译）。
- 不在生产环境启用 dev/staging 测试账号。
- 不只提供 Google 登录作为 iOS 公开版本的唯一登录方式。
- 不做首版学习数据云同步。

## 验收指标

自用 / TestFlight 阶段只看行为指标，不用空泛的“体验好”来判断。

- 连续 7 天中至少 5 天打开 App。
- 每天至少完成 1 个真实英文网页的翻译浏览。
- 每天至少收藏 3 条词、短语或句子。
- 每天至少完成 1 轮复习。
- 网页翻译失败时，用户能明确知道失败原因，并能使用选区翻译降级。
- YouTube 整站能像原生一样正常操作（上下滑视频列表、点开视频 / Shorts 不报错、不卡）；视频字幕不可用时，Free 用户能看到听音翻译 Beta 入口和今日 10 分钟额度状态。

## 参考依据

- Immersive Translate：网页双语翻译、主流网站优化、移动端支持、视频字幕翻译。
- Trancy：YouTube / Netflix 双语字幕、字幕阅读模式、AI 查词和网页翻译。
- Readlang：点词点句翻译、收藏、flashcard 复习。
- Language Reactor ReadLang 1L：移动端 Web + Video context learning、主动回忆、间隔重复。
- Apple App Review Guidelines：App 不能只是网页套壳或链接集合，必须有足够原生功能。
- Apple WKUserScript：iOS WebView 页面脚本注入基础能力。
- YouTube API Services Developer Policies：不能修改播放器功能、去广告、下载或分离音视频内容。
- 便宜大模型 API（DeepSeek / Kimi / GLM / Qwen 等，OpenAI 兼容 Chat Completions）：Free 文本翻译后端，由产品方在服务端配置，注册充值简单、成本低、翻译质量优于通用机器翻译；具体模型与限额由后台成本策略决定。
- Azure AI Translator / Google Cloud Translation：通用机器翻译曾作为 Free 候选；因账号申请成本高、质量不及大模型，本版改用便宜大模型，保留为后续可选的通用翻译通道。
- Apple App Review Guidelines 4.8：使用第三方 / 社交登录作为主账号登录时，需要提供等价的隐私登录选项；账号创建也必须支持 App 内删除。
- Google Sign-In for iOS backend auth：客户端登录后应把 ID token 发送到后端验证，后端不能接受客户端传来的普通 user id 作为身份依据。
