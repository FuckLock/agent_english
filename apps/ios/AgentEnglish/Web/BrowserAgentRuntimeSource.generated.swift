import Foundation

enum BrowserAgentRuntimeSource {
    // Generated from packages/browser-agent/src/browser-runtime-source.ts.
    static let source = #"""
(()=>{const bridge=window.webkit?.messageHandlers?.__HANDLER_NAME__;if(!bridge||window.__agentEnglishBridgeBootstrapped)return;window.__agentEnglishBridgeBootstrapped=true;const schemaVersion=1;const capabilities=["readable-page","inline-translation","selection-fallback"];const displayModes={original:"original",bilingual:"bilingual",learning:"learning"};const expandedSegmentIds=new Set();const overlaysBySegmentId=new Map();const anchorsBySegmentId=new Map();const blockTags=new Set(["ARTICLE","ASIDE","BLOCKQUOTE","DIV","FIGCAPTION","FOOTER","HEADER","LI","MAIN","NAV","P","SECTION"]);const pageNoticeId="agent-english-page-notice";let currentDisplayMode=displayModes.original;const sessionId=(typeof crypto!=="undefined"&&crypto.randomUUID)?crypto.randomUUID():"session-"+Date.now()+"-"+Math.random().toString(16).slice(2);const pageId="page-"+sessionId;const postBridgeEvent=(eventType,payload,metadata={})=>bridge.postMessage({schemaVersion,eventType,requestId:metadata.requestId,pageId,payload,result:metadata.result,error:metadata.error});const normalizeText=(text)=>(text??"").replace(/\s+/g," ").trim();const hashSeed=(seed)=>{let hash=2166136261;for(let index=0;index<seed.length;index+=1){hash^=seed.charCodeAt(index);hash=Math.imul(hash,16777619);}return"seg-"+(hash>>>0).toString(16);};const failureMessage=(failureReason)=>{switch(failureReason){case"provider-not-configured":return"Configure a provider to translate this page.";case"page-unrecognized":return"Select text to translate on this page.";default:return"Translation is unavailable right now.";}};const clearPageNotice=()=>{const notice=document.getElementById(pageNoticeId);if(notice)notice.remove();};const ensurePageNotice=()=>{let notice=document.getElementById(pageNoticeId);if(!notice){notice=document.createElement("div");notice.id=pageNoticeId;notice.style.margin="12px";notice.style.padding="10px 12px";notice.style.borderRadius="12px";notice.style.background="rgba(245, 158, 11, 0.12)";notice.style.color="#92400e";document.body?.insertAdjacentElement("afterbegin",notice);}return notice;};const isVisible=(element)=>{let currentElement=element;while(currentElement){const style=getComputedStyle(currentElement);if(style.display==="none"||style.visibility==="hidden"||style.opacity==="0")return false;const rect=currentElement.getBoundingClientRect();if(rect.width<=0&&rect.height<=0)return false;currentElement=currentElement.parentElement;}return true;};const siblingIndex=(element)=>{if(!element.parentElement)return 1;const siblings=Array.from(element.parentElement.children).filter((sibling)=>sibling.tagName===element.tagName);return Math.max(1,siblings.indexOf(element)+1);};const elementPath=(element)=>!element.parentElement?element.tagName.toLowerCase():elementPath(element.parentElement)+">"+element.tagName.toLowerCase()+":nth-of-type("+siblingIndex(element)+")";const blockContainer=(element)=>{let currentElement=element;while(currentElement?.parentElement){if(blockTags.has(currentElement.tagName))return currentElement;const display=getComputedStyle(currentElement).display;if(display==="block"||display==="flex"||display==="grid"||display==="list-item")return currentElement;currentElement=currentElement.parentElement;}return currentElement??element;};const scanPage=(config)=>{const groups=new Map();anchorsBySegmentId.clear();const walker=document.body?document.createTreeWalker(document.body,NodeFilter.SHOW_TEXT):null;if(!walker)return null;let textNode=walker.nextNode();while(textNode){const normalizedText=normalizeText(textNode.textContent);const parentElement=textNode.parentElement;if(normalizedText&&parentElement&&isVisible(parentElement)){const anchorElement=blockContainer(parentElement);const containerPath=elementPath(anchorElement);const existingGroup=groups.get(containerPath)??{anchorElement,containerPath,sourceTextParts:[]};existingGroup.sourceTextParts.push(normalizedText);groups.set(containerPath,existingGroup);}textNode=walker.nextNode();}const segments=Array.from(groups.values()).map((group)=>{const sourceText=normalizeText(group.sourceTextParts.join(" "));const segmentId=hashSeed(pageId+":"+group.containerPath+":"+sourceText);anchorsBySegmentId.set(segmentId,group.anchorElement);return{pageId,segmentId,sourceText,containerPath:group.containerPath,sourceLanguage:config.sourceLanguage,isVisible:true,capabilities};});return{pageContext:{pageId,url:window.location.href,title:document.title||"",sourceLanguage:config.sourceLanguage,targetLanguage:config.targetLanguage,displayMode:config.displayMode,capabilities,siteKind:"generic"},segments};};const overlayHidden=(segmentId)=>currentDisplayMode===displayModes.original?true:currentDisplayMode===displayModes.learning?!expandedSegmentIds.has(segmentId):false;const ensureOverlay=(segmentId)=>{const anchorElement=anchorsBySegmentId.get(segmentId);if(!anchorElement)return null;let overlay=overlaysBySegmentId.get(segmentId);if(overlay?.parentElement)return overlay;overlay=document.createElement("div");overlay.dataset.agentEnglishSegmentId=segmentId;overlay.className="agent-english-translation-overlay";overlay.style.marginTop="8px";overlay.style.fontSize="0.92em";overlay.style.lineHeight="1.5";overlay.style.color="#475569";overlay.addEventListener("click",()=>{if(currentDisplayMode!==displayModes.learning)return;if(expandedSegmentIds.has(segmentId)){expandedSegmentIds.delete(segmentId);}else{expandedSegmentIds.add(segmentId);}syncDisplayMode();});anchorElement.insertAdjacentElement("afterend",overlay);overlaysBySegmentId.set(segmentId,overlay);return overlay;};const renderFailure=(failurePayload)=>{clearPageNotice();if(failurePayload.segmentId){const overlay=ensureOverlay(failurePayload.segmentId);if(overlay){overlay.textContent=failureMessage(failurePayload.failureReason);overlay.hidden=false;overlay.title=overlay.textContent;overlay.style.color="#92400e";return;}}const notice=ensurePageNotice();notice.textContent=failureMessage(failurePayload.failureReason);};const syncDisplayMode=()=>{overlaysBySegmentId.forEach((overlay,segmentId)=>{overlay.hidden=overlayHidden(segmentId);});};const requestTranslation=(config={})=>{const displayMode=config.displayMode===displayModes.original?displayModes.bilingual:(config.displayMode||currentDisplayMode||displayModes.bilingual);currentDisplayMode=displayMode;clearPageNotice();expandedSegmentIds.clear();const scanResult=scanPage({sourceLanguage:config.sourceLanguage||"English",targetLanguage:config.targetLanguage||"简体中文",displayMode});if(!scanResult||!scanResult.segments.length){const failurePayload={pageId,sourceLanguage:config.sourceLanguage||"English",targetLanguage:config.targetLanguage||"简体中文",displayMode,capabilities,failureReason:"page-unrecognized"};renderFailure(failurePayload);postBridgeEvent("translation.failed",failurePayload,{requestId:"translation-failed-"+sessionId,pageId,error:{code:"page.unrecognized",message:"No readable text segments found."}});return false;}const requestPayload={pageId,pageContext:scanResult.pageContext,sourceLanguage:scanResult.pageContext.sourceLanguage,targetLanguage:scanResult.pageContext.targetLanguage,displayMode,capabilities,segments:scanResult.segments};postBridgeEvent("translation.requested",requestPayload,{requestId:"translation-requested-"+sessionId,pageId});return true;};const applyTranslationResult=(translationResult)=>{currentDisplayMode=translationResult.displayMode||currentDisplayMode;clearPageNotice();for(const segmentResult of translationResult.segmentResults||[]){const overlay=ensureOverlay(segmentResult.segmentId);if(!overlay)continue;overlay.textContent=segmentResult.failureReason?failureMessage(segmentResult.failureReason):(segmentResult.translatedText||"Translation ready");overlay.title=segmentResult.failureReason?overlay.textContent:"";overlay.style.color=segmentResult.failureReason?"#92400e":"#475569";}syncDisplayMode();postBridgeEvent("translation.completed",translationResult,{requestId:"translation-completed-"+sessionId,pageId:translationResult.pageId});return true;};const applyTranslationFailure=(failurePayload)=>{renderFailure(failurePayload);postBridgeEvent("translation.failed",failurePayload,{requestId:"translation-failed-"+sessionId,pageId:failurePayload.pageId,error:{code:failurePayload.failureReason,message:failureMessage(failurePayload.failureReason)}});return true;};const setDisplayMode=(mode)=>{currentDisplayMode=mode||displayModes.original;syncDisplayMode();return currentDisplayMode;};window.__agentEnglishBridge={requestTranslation,applyTranslationResult,applyTranslationFailure,setDisplayMode};postBridgeEvent("bridge.boot",{sessionId,bridgeScope:"bootstrap"},{requestId:"boot-"+sessionId});postBridgeEvent("bridge.ping",{sessionId,sentAt:new Date().toISOString()},{requestId:"ping-"+sessionId,result:{acknowledged:false}});const postPageReady=()=>{postBridgeEvent("page.ready",{sessionId,url:window.location.href,title:document.title||"",loadedAt:new Date().toISOString()},{requestId:"page-ready-"+sessionId});};if(document.readyState==="loading"){document.addEventListener("DOMContentLoaded",postPageReady,{once:true});}else{postPageReady();}})();
"""#
}
